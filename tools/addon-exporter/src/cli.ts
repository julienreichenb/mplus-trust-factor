#!/usr/bin/env node
/**
 * Addon export CLI skeleton — Agent 7 owns real Lua shard generation.
 */
console.log(
  JSON.stringify(
    {
      status: "not_implemented",
      message: "Addon exporter skeleton only. Owned by Agent 7.",
    },
    null,
    2,
  ),
);
process.exitCode = 0;
